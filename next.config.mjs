/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:["badidukkan.in"]
    }
};

export default nextConfig;

// /** @type {import('next').NextConfig} */

// const nextConfig = {
//     distDir: 'build',
//     images:{
//         domains:["badidukkan.in"]
//     }
// }

// module.exports = nextConfig